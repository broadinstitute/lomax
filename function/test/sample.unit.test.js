'use strict';

const assert = require('assert');

describe('dummy test to ensure test runner is functional', () => {
  it('should pass', () =>{
    const testPasses = true;

    assert(testPasses);
  });
});
